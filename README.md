Flat map
=====================

### Run demo

```
npm install
npm start
open http://localhost:3000
```


### Build and run

```
npm install
npm run build
cd build
python -m SimpleHTTPServer
open http://localhost:8000
```